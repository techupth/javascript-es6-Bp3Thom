let height = undefined;
let result;

function check(height)
{
    if(height === null ?? height === 'Height is not defined') {
        return height;
    }
return result;
}

console.log(result); //  Result ควรจะได้ออกมาเป็น “Height is not defined”
